# Текущее состояние проекта

## Реализовано

- Начальная интеграция Todoist.
- Нормализованный `tasks.json`.
- Предыдущий снимок `tasks.prev.json`.
- Детерминированный `tasks.diff.json`.
- Структура постоянной памяти.
- Правила approve для основной памяти.
- `context_builder.py`, собирающий память и Todoist в единый контекст.

## Текущий этап

Слой памяти и сборки контекста сформирован.

## Следующий крупный этап

Слой работы агента с LLM:
- единый интерфейс вызова сменной LLM;
- сценарий вечерней сверки;
- разбор свободной речи;
- формирование плана;
- запись дневного журнала;
- генерация предложений изменений core;
- применение изменений после approve.
